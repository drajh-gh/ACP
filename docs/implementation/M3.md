# M3 — Project profiles and onboarding

- Status: In progress; independent local work alongside unfinished M2.
- Specification: [1.0.0](../../SPECIFICATION.md), §13 and M3 milestone.
- Domain checkpoint: `27791466cf61c4e2737a83fe62a3ea6848622534` (local).
- Storage checkpoint: `89bdf80593eb372f5ffdf13a17789b32c27eeab8` (local).
- Readiness MCP checkpoint: `449bd9e282c031cf912ecb0b309ea17cfeb67b89` (local).
- Proposal domain checkpoint: `86909a801c67490495af62ea96f60b824ae2aa92` (local).

## Implemented readiness foundation

- [x] Add exact project/profile/resource-scoped readiness query and immutable-
      shaped assessment contracts, leaving the legacy readiness DTO unchanged.
- [x] Retain all seven recorded states separately from derived current state;
      absent observations remain unknown, positive expiry/evidence drift becomes
      stale, and expiry never clears revocation.
- [x] Preserve canonical UTC microseconds, exact expiry boundaries, explicit
      predecessor identity, unique sorted evidence IDs and a 24-hour validity cap.
- [x] Build one bounded metadata-only matrix from already-selected latest rows;
      deny invalid, restricted, cross-project or missing references as a whole.
- [x] Revalidate response identity and recompute state before accepting a matrix;
      never imply live probes, profile activation, grants or aggregate readiness.
- [x] Persist immutable exact assessment history and normalized server-derived
      evidence pins, with active provenance/binding admission and a default
      deferred commit guard in the private trusted producer.
- [x] Enforce exact predecessor compare-and-swap, unique root/predecessor/time
      constraints, physical transaction disposal and confirmed historical replay
      after same-ID races or uncertain COMMIT acknowledgement.
- [x] Read one bounded PostgreSQL snapshot using exact profile existence,
      latest-before-filtering selection and current evidence checks; expose
      identity drift separately from freshness/accessibility/hash metadata.
- [x] Make empty downgrade reversible while refusing removal of retained history.
- [x] Expose authenticated read-only `get_project_readiness` with strict input,
      exact schema-2 response validation and constant whole-tool error masking.
- [x] Add the tool to the repository-local counterpart package and document
      observation-versus-authority boundaries; advertise control/plugin `0.2.0`
      while preserving default `0.1.0` compatibility and explicit operator lists.

The internal writer remains a trusted application boundary, not an evaluator-role
registry or model-facing writer. The MCP read uses the existing global trusted
bearer-client boundary, not a new per-project user ACL. See
[ADR0019](../architecture/0019-recorded-capability-readiness.md) for the exact
contract, default-deferral/privileged-SQL caveat and remaining authority boundary.

## Verification

- Test-first readiness module import initially failed before implementation.
- Twelve focused domain tests cover exact scopes, timestamp precision/date
  validity, all states, absence versus unconfigured, expiry/revocation, evidence
  deterioration, generic denial, metadata-only output, response substitutions,
  identity drift and whole-response UTF-8 bounds. Identity-drift RED demonstrated
  an incorrectly available state before the explicit comparison was implemented.
  Schema-version RED/GREEN requires snapshot `2.0.0` and rejects the previous
  domain-only version instead of inventing legacy evidence identity matches.
- Five local storage tests cover trusted evaluator identity, bounded pools,
  read-only query behavior, exact replay and uncertain-session disposal.
- Real PostgreSQL readiness phases: core 8, races 8, expiry 6, limits 6,
  snapshot 2 and upgrade 2 checks pass (32 total). They use seeded schema through
  0017 and no live provider probes. Evidence includes concurrent roots/successors,
  repeatable-read denial, real lost-COMMIT readback and backend closure, same-
  transaction drift/retirement, forced early constraint checks, Unicode bounds,
  concurrent MVCC observations and history-preserving downgrade.
- Prior counterpart-status core (7) and inert provisioner-plan core (9) pass
  under 0017. All 20 migration-session checks pass, including apply/checksum replay/
  full reverse of all 17 actual migration pairs on an empty database.
- Strict TypeScript, all 391 local tests, secret scan and diff hygiene pass.
  Independent functional/specification and repository-conformance reviews pass;
  the final core rerun also verifies snapshot `2.0.0` through real storage. Reviewers
  did not rerun the main agent's sequential gates. No credentials, live models or
  external effects are involved; all disposable containers/process trees are owned,
  bounded and independently checked absent after cleanup.

### Read-only MCP verification

- Test-first RED demonstrated the absent tool and rejected new plugin version;
  a separate versioning RED caught unchanged MCP/health server advertisements.
- Seven HTTP tests cover both compatible plugin versions, strict queries with no
  invalid-input persistence calls, independent states, whole-error masking,
  private exception sentinels, schema/key/scope substitutions, advertised server
  version, explicit operator restrictions and source package/header/tool alignment.
- The actual PostgreSQL/HTTP phase passes eight checks with a SELECT-only role
  and read-only transactions. The route cannot create database objects; it keeps
  all seven states independent, returns unknown for case-distinct unobserved keys,
  reports positive identity drift as stale, and denies restricted/cross-project
  evidence indistinguishably from absent exact profiles. Read-side durable counts
  remain unchanged. The fixture never starts a live model or deployed server.
- Strict TypeScript and all 398 local tests pass. The plugin-creator manifest
  validator and skill-creator skill validator both pass on the local source package.
- The final version-adjusted PostgreSQL/HTTP rerun, secret scan and diff hygiene
  pass. Independent functional/specification and repository-conformance reviews
  pass on the exact implementation; reviewers ran no heavy gates. Both disposable
  database containers and all owned process trees were checked absent afterward.

## Inert profile-proposal domain foundation

- [x] Address all 62 fields in the 15 specification areas, keeping discovered,
      proposed, missing, conflicted and proposed non-applicability separate.
- [x] Bind exact project/candidate/base/predecessor/producer metadata, all fields
      and evidence fingerprints to a canonical bounded proposal digest.
- [x] Derive a complete read-only confirmation request only when every field is
      addressed and trusted current snapshot facts match; never claim a persisted
      operator decision, authority narrowing, publication or activation.
- [x] Keep credential references/read-write identities structural and tracker
      mappings exact without prescribing a universal native workflow.

This checkpoint is pure domain machinery, not discovery I/O, a proposal ledger, a complete
runtime-profile compiler, or authenticated operator confirmation. Most values
are bounded inert review JSON; non-secret text is a trusted-producer responsibility,
not something the parser can prove. The existing active-profile shape, root
`sourceAuthorityRules`, node-context limits and workflow bindings are untouched.
See [ADR0020](../architecture/0020-inert-profile-proposals.md) for exact boundaries.

Twelve focused proposal tests and one shared exact-time test cover field/gap
coverage, evidence and digest identity, credential/tracker boundaries, whole-
request denial, bounds, sparse arrays and canonical numeric round-trips. Test-first
REDs also reproduced coerced sensitivity, omitted sensitivity in request identity,
duplicate credential identities, sparse evidence and negative-zero aliasing.
The parallel strictness defects in readiness received their own RED regressions.
Final verification passes: all 411 local tests, strict TypeScript, secret scan,
diff hygiene, and real PostgreSQL readiness core (8) plus HTTP (8) regressions.
Independent functional/specification and repository-conformance reviews pass on
the exact final source. Reviewers ran no gates. Owned processes and disposable
databases were checked absent after bounded cleanup. These database regressions
cover existing readiness compatibility, not persistence of the new proposals.

## Retained inert profile-proposal ledger

- [x] Add migration 0018 with immutable proposal/correction history and normalized
      server-derived evidence pins, separate from published runtime profiles.
- [x] Enforce one exact candidate chain, latest-predecessor compare-and-swap,
      same-project evidence admission and default deferred identity rechecks.
- [x] Bind the private producer, reject caller-owned authority/timestamps and
      recover exact historical retries after same-ID races or lost COMMIT responses.
- [x] Contain checked-out socket errors, discard physical sessions before readback
      and preserve uncertainty when independent readback itself fails.
- [x] Build disclosure-checked historical reads and current confirmation requests
      from one MVCC snapshot, without inferring profile-head or publication authority.
- [x] Keep PostgreSQL baseline/storage checksums distinct from the proposal's
      JavaScript digest; reject lossy numeric parsing from raw stored JSON text.
- [x] Bound body and metadata separately, preserve near-limit quoted text, reject
      server-pin expansion beyond the canonical body limit and retain history on downgrade.

See [ADR0021](../architecture/0021-retained-profile-proposals.md). The writer is
internal only and the reads are not yet exposed as MCP tools. Discovery I/O,
operator confirmation, runtime-profile compilation, publication and activation
remain separate work. Existing active-profile and workflow-binding shapes are unchanged.

Ledger verification passes six local storage/parser tests, strict TypeScript and
all 417 local tests. Test-first failures demonstrated the missing store, missing
checked-out socket listener, lossy direct-JSON decimal disclosure and valid large
documents rejected by double transport escaping. The last two have real PostgreSQL
RED/GREEN regressions. Exact-number controls include scale/exponent variants,
Unicode, signed zero, maximum-safe integers and subnormal doubles.

Final real PostgreSQL proposal phases pass core 9, races 8, references 11,
snapshot 4 and upgrade 2 checks (34 total). They include terminated fixture backends
after INSERT/COMMIT, lost historical-replay acknowledgements, concurrent roots and
successors, repeatable-read stale snapshots, private exact retries after restriction,
same-transaction/forced-early evidence checks, no-partial disclosure, near-limit
quote-heavy valid documents, post-INSERT pin-expansion rollback and a SELECT-only
reader. Concurrent correction, restriction and publication each leave the in-flight
MVCC observation coherent while the next request rejects.

The prior readiness core and actual read-only PostgreSQL/HTTP route each pass eight
checks under 0018. All 20 migration-session checks pass, including all 18 actual
migration pairs applying, checksum-replaying and reversing on an empty database.
Secret scan and diff hygiene pass. Independent functional/specification and
repository-conformance reviews pass on the final source; reviewers ran no heavy
gates. Disposable database containers and owned process trees are removed and
independently checked absent. No production data, live model, deployment, plugin
installation or external effect is involved.

## Readiness connection-loss follow-up

The readiness writer now observes checked-out socket errors and stops subsequent
SQL, keeping its listener until physical discard. Exact historical retries also
recover after a lost read-only COMMIT acknowledgement, without rerunning current
admission, refreshing pins or extending validity. Two targeted RED tests reproduced
the missing listener and failed historical-replay recovery before the fix.

Seven targeted storage tests, strict TypeScript and all 419 local tests pass.
Real PostgreSQL readiness races now pass 11 checks, including actual terminated
backends after INSERT and COMMIT and lost historical-replay acknowledgements.
Readiness core (8) and actual read-only PostgreSQL/HTTP (8) pass under 0018.
Secret scan, diff hygiene and independent read-only functional/conformance review
pass. Owned process trees and disposable database containers are checked absent
after cleanup. No schema, API, recorded-state or authority semantics change.

## Remaining


- [ ] Role-bound evaluator authorization beyond the trusted internal writer.
- [ ] Whole-profile discovery and explicit user confirmation flow.
- [ ] Typed runtime-profile compilation, authority-delta evaluation and publication.
- [ ] Project-specific lifecycle maps and completion contracts.
- [ ] A real SloSki profile, built from confirmed non-secret project facts.
- [ ] Demonstrated end-to-end onboarding with independent ready/degraded/blocked/
      stale/unknown capability distinctions. Domain tests alone are not this exit gate.

M2 deployment, plugin installation, actual worker sandbox evidence and unfinished
provisioner lifetime/write-authority contracts remain open in [M2](M2.md).
