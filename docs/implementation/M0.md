# M0 — Repository and architectural invariants

- Status: Locally complete; external CI evidence pending
- Baseline: `main` at `1370b5fbee7066ef0757d07d00833ce77fe9a810`
- Implementation checkpoint: `7c05911d64fce63a03786a661e9f3df01561898e` (local)
- Specification: [1.0.0](../../SPECIFICATION.md)

## Implemented in the initial slice

- [x] Preserve the supplied repository as a project independent of Poppy.
- [x] Add a TypeScript npm-workspaces baseline and strict compiler policy.
- [x] Record the fixed architecture decisions for TypeScript, PostgreSQL, DBOS,
      Codex SDK workers, the authenticated remote MCP plugin, and effect
      mediation.
- [x] Introduce opaque stable identifiers with entity-specific prefixes.
- [x] Model mission, candidate, deployment, acceptance, tracker, effect, and
      business-completion state as orthogonal facts.
- [x] Require a passed, versioned completion evaluation before recording
      `complete_for_scope`.
- [x] Reject scoped completion while an effect has `unknown_outcome`.
- [x] Add local tests proving lifecycle separation.
- [x] Add a bounded, dependency-free candidate secret scan.
- [x] Add a five-minute GitHub Actions gate for tests and secret scanning.
- [x] Add runtime-validated workflow, intake, claim/evidence, approval, effect,
      capability, readiness, and reconciliation contracts, plus typed
      context-packet, worker-result, and provenance contracts.

## Follow-up gates

- [x] Pin the approved TypeScript compiler/tooling dependencies and activate a
      compiler-based type-check gate.
- [ ] Observe the GitHub-hosted CI result on an exact pushed revision.
- [ ] Replace the baseline pattern scanner with a mature secret-scanning tool
      when that dependency is approved.

## M0 exit evidence

| Exit criterion | Current evidence |
|---|---|
| Domain tests establish lifecycle separation | Local `npm test`: lifecycle suite passes within 107 total tests |
| Type contracts compile under the pinned toolchain | Local `npm run typecheck`: passed with TypeScript 7.0.2 and Node types 24.13.3 |
| Repository contains no live credentials or project-confidential examples | Local `npm run check:secrets`: passed; remote CI not yet observed |

The checklist tracks implementation evidence only. It does not authorize a
commit, push, pull request, deployment, credential setup, or other external
effect.
